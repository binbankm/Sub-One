# Vue 3 标准架构重构实施计划

> 创建时间：2026-01-14
> 状态：待审核

---

## 📋 重构目标

将当前分散的前端代码按照 **Vue 3 推荐的项目结构** 进行重组，提升代码的：

- 可维护性
- 可读性
- 可扩展性
- 团队协作效率

---

## 🏗️ 目标目录结构

```
src/
├── assets/                      # 静态资源
│   └── styles/
│       └── main.css
│
├── components/                  # 全局通用组件（无业务逻辑）
│   ├── ui/                      # UI 基础组件
│   │   ├── BaseModal.vue
│   │   ├── ConfirmModal.vue
│   │   ├── Toast.vue
│   │   ├── Pagination.vue
│   │   └── EmptyState.vue
│   ├── layout/                  # 布局组件
│   │   ├── AppSidebar.vue
│   │   └── AppFooter.vue
│   └── charts/                  # 图表组件
│       └── (existing)
│
├── composables/                 # 组合式函数（可复用逻辑）
│   ├── useBatchSelection.ts     # 批量选择逻辑
│   └── useAppPersistence.ts     # 数据持久化逻辑
│
├── pages/                       # 页面视图（新建）
│   ├── LoginPage.vue            # 登录页（从 views/LoginView.vue 移动）
│   └── DashboardPage.vue        # 仪表盘页面（简化后的协调器）
│
├── features/                    # 功能模块（按业务域划分）
│   ├── subscriptions/           # 订阅管理模块
│   │   ├── components/
│   │   │   ├── SubscriptionCard.vue
│   │   │   ├── SubscriptionEditModal.vue
│   │   │   └── SubscriptionImportModal.vue
│   │   ├── SubscriptionsTab.vue
│   │   └── useSubscriptions.ts  # 订阅相关逻辑
│   │
│   ├── profiles/                # 订阅组管理模块
│   │   ├── components/
│   │   │   ├── ProfileCard.vue
│   │   │   └── ProfileModal.vue
│   │   ├── ProfilesTab.vue
│   │   └── useProfiles.ts
│   │
│   ├── nodes/                   # 手动节点管理模块
│   │   ├── components/
│   │   │   ├── ManualNodeCard.vue
│   │   │   ├── NodeEditModal.vue
│   │   │   └── NodeDetailsModal.vue
│   │   ├── NodesTab.vue
│   │   └── useManualNodes.ts
│   │
│   ├── generator/               # 链接生成模块
│   │   ├── components/
│   │   │   └── SubscriptionLinkGeneratorCard.vue
│   │   └── GeneratorTab.vue
│   │
│   ├── settings/                # 设置模块
│   │   ├── SettingsModal.vue
│   │   └── HelpModal.vue
│   │
│   └── dashboard/               # 仪表盘首页模块
│       └── DashboardHome.vue
│
├── stores/                      # Pinia 状态管理（保持现有结构）
│   ├── session.ts               # 会话状态 ✅ 保持
│   ├── theme.ts                 # 主题状态 ✅ 保持
│   ├── layout.ts                # 布局状态 ✅ 保持
│   ├── toast.ts                 # 消息提示 ✅ 保持
│   └── ui.ts                    # UI 状态 ✅ 保持
│
├── utils/                       # 纯工具函数（合并 lib + utils）
│   ├── api.ts                   # API 请求
│   ├── constants.ts             # 常量定义
│   ├── importer.ts              # 导入解析
│   ├── format.ts                # 格式化工具
│   ├── node.ts                  # 节点处理
│   ├── protocols.ts             # 协议处理
│   ├── search.ts                # 搜索工具
│   └── index.ts                 # 统一导出
│
├── types/                       # 类型定义（新建目录）
│   └── index.ts                 # 从 types.ts 移动
│
├── App.vue                      # 根组件（简化）
├── main.ts                      # 入口文件
└── vite-env.d.ts               # Vite 类型声明
```

---

## 📝 具体任务清单

### 阶段一：基础结构调整（低风险）

| # | 任务 | 说明 | 优先级 |
|---|------|------|--------|
| 1.1 | 创建 `types/` 目录 | 将 `types.ts` 移动到 `types/index.ts` | 🟢 高 |
| 1.2 | 合并 `lib/` 到 `utils/` | 将 `src/lib/*.ts` 移动到 `src/utils/` | 🟢 高 |
| 1.3 | 删除空的 `src/lib/` 目录 | 清理旧目录 | 🟢 高 |
| 1.4 | 创建 `utils/index.ts` | 统一导出所有工具函数 | 🟡 中 |

### 阶段二：组件重组（中风险）

| # | 任务 | 说明 | 优先级 |
|---|------|------|--------|
| 2.1 | 创建 `components/ui/` | 移动通用 UI 组件 | 🟢 高 |
| 2.2 | 创建 `pages/` 目录 | 创建页面级组件 | 🟢 高 |
| 2.3 | 创建 `features/` 目录结构 | 按业务模块组织 | 🟢 高 |
| 2.4 | 移动订阅相关组件 | `cards/SubscriptionCard.vue` → `features/subscriptions/components/` | 🟡 中 |
| 2.5 | 移动订阅组相关组件 | `cards/ProfileCard.vue` → `features/profiles/components/` | 🟡 中 |
| 2.6 | 移动节点相关组件 | `cards/ManualNodeCard.vue` → `features/nodes/components/` | 🟡 中 |

### 阶段三：Composables 重组（中风险）

| # | 任务 | 说明 | 优先级 |
|---|------|------|--------|
| 3.1 | 移动业务 Composables | 将业务相关的 composables 移动到对应的 features 目录 | 🟡 中 |
| 3.2 | 保留通用 Composables | `useBatchSelection.ts` 和 `useAppPersistence.ts` 保留在 `composables/` | 🟡 中 |

### 阶段四：简化 DashboardView（高风险）

| # | 任务 | 说明 | 优先级 |
|---|------|------|--------|
| 4.1 | 拆分 DashboardView | 将 1040 行的文件拆分为多个独立组件 | 🔴 高 |
| 4.2 | 创建 DashboardPage | 简化为只负责协调各个 Tab 的切换 | 🔴 高 |

### 阶段五：更新导入路径（必须步骤）

| # | 任务 | 说明 | 优先级 |
|---|------|------|--------|
| 5.1 | 更新所有导入路径 | 全局搜索替换旧路径 | 🔴 高 |
| 5.2 | 验证构建 | 确保 `npm run build` 成功 | 🔴 高 |
| 5.3 | 验证类型检查 | 确保 `npm run type-check` 成功 | 🔴 高 |

---

## ⚠️ 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 导入路径大量更新 | 可能遗漏 | 使用 IDE 重构功能 + 构建验证 |
| 组件依赖关系复杂 | 拆分困难 | 分阶段执行，每阶段验证 |
| 回归测试不足 | 功能损坏 | 先手动测试核心流程 |

---

## ✅ 验收标准

1. ✅ 所有文件按新目录结构组织
2. ✅ `npm run dev` 正常启动
3. ✅ `npm run build` 成功构建
4. ✅ `npm run type-check` 无类型错误
5. ✅ 核心功能正常（登录、订阅管理、生成链接）

---

## 🤔 待确认问题

在开始执行前，请确认以下几点：

1. **是否保留 `editors/` 目录？** 当前只有 1 个文件，是否合并到其他目录？

2. **charts 组件如何处理？** 是保留在 `components/charts/` 还是移动到 `features/dashboard/components/`？

3. **BulkImportModal.vue** 应该放在哪个 feature 下？（订阅和节点都可能用到）

4. **执行策略选择：**
   - **A. 一次性完整重构** - 风险高，但一步到位
   - **B. 分阶段渐进重构** - 风险低，每阶段验证

---

**请审核以上计划，确认后我将开始执行。**
