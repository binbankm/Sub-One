# ==========================================
# Sub-One 配置文件示例
# ==========================================
# 复制此文件为 .env 并修改配置
# cp .env.example .env

# 服务运行端口（宿主机端口，可自定义避免冲突）
PORT=3055

# 时区设置（容器内时区）
TZ=Asia/Shanghai

# 管理员后台密码（！重要：生产环境务必修改！）
ADMIN_PASSWORD=admin

# 数据存储路径（容器内固定为 /app/data，宿主机通过 docker-compose.yml 映射）
DATA_DIR=/app/data

# 运行环境 (development / production)
NODE_ENV=production

# ==========================================
# 可选配置
# ==========================================

# Redis 数据库连接（可选，留空则使用本地文件存储）
# 启用Redis需要两步：
# 1. 取消下面这行的注释
# 2. 在 docker-compose.yml 中取消 Redis 服务及相关配置的注释
# 3. 重启：docker-compose down && docker-compose up -d
#
# REDIS_URL=redis://redis:6379

# ==========================================
# VPS 部署步骤
# ==========================================
# 1. 复制并修改配置文件：
#    cp .env.example .env
#    nano .env  # 修改 ADMIN_PASSWORD
#
# 2. 启动服务：
#    docker-compose up -d
#    （数据目录会自动创建，也可手动创建：mkdir -p /root/sub-one-data）
#
# 3. 查看日志：
#    docker-compose logs -f


